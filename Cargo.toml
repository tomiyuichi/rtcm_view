[package]
name = "rtcm_view"
version = "1.0.0"
edition = "2024"

[dependencies]
eframe = "0.27"
image = { version = "0.24", default-features = false, features = ["png"] }

[build-dependencies]
cc = "1"
winres = "0.1"
